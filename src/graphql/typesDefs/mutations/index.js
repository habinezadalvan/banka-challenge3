import { userMutations } from './user.mutations';
import { contributionMutation } from './contribution.mutations';
import { loanMutations } from './loan.mutations';


export const allMutations = {
  userMutations,
  contributionMutation,
  loanMutations,
};
